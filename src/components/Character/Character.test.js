// import { render, fireEvent, waitFor } from '@testing-library/react';
// import Character from './Character.js';

// describe('Character', () => {
//   let container;
//   beforeEach(() => (container = render(<Character />)));

//   it('should render characters on the page', () => {
//     const name = container.getByTestId('name-test');
//     expect(name.textContent).toEqual('Rick Sanchez');
//   });
// });
